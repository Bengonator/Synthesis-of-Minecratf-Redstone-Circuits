
-warum darf nach generate-forms nix mehr stehen im 'if', da er sonst nix generiert?

-das bound verringern noch optimieren
zb 1 mit max bound, damit man unsat gleich erkennt
dann mit 50% bound, wenn unsat dann 75%, sonst 25% (also binary search)

-falls sinnvoll, dann am ende (bzw vor dem "minimized print") die hashwerte auslesen und wirklich in eine liste speichern, so dass man mehr damit tun kanns
